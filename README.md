# DS-Algorithm-Analysis-HW1

Repo for data structures homework 1

# User Guide

1. To run the code for each task, `cd` into the folder for that task
2. In the terminal, type `build` to compile
3. In the terminal, type `build.\programe.exe` to run the generated executable.

All auto generated files are created in the `build` folder for each task.
